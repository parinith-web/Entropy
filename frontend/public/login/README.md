# Login page (Figma replica)

Files in this folder:

- `index.html` — static login page that mirrors the Figma design.
- `styles.css` — page styles.
- `app.js` — minimal form demo behavior.

Preview locally:

On Windows, open `c:\Users\parin\Downloads\MAS\frontend\login\index.html` in your browser.

Notes:
- Images are now downloaded into the `assets/` folder and referenced locally in `index.html` so the page works offline.
- If you want to refresh assets from Figma, delete `assets/` and re-run the asset download step.
- This is a static demo — integrate authentication as needed.

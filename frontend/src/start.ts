// start.ts is not used in SPA mode.
// This was the TanStack Start middleware entry for SSR request handling.
// In SPA mode, there is no server-side middleware — all routing is client-side.
export {};
